package out.serializer;

interface TreeSerializer {
    // serialize will take the root of a binary tree and serialize it into an int[]
    public int[] serialize(Node root);

    //TO DO
    //public int[] deserialize(int[] nodes);
    //DFS tree traversal
}
